export interface IAura {
  canTransform: boolean
  fill: string // hexcode e.g. "#fff"
  height: number
  id: string
  stroke: string // hexcode e.g. "#fff"
  strokeWidth: number
  width: number
  x: number
  y: number
}
